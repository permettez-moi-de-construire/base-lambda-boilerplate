const {
  describe,
  it
} = require('mocha')

const assert = require('assert')

describe('MyPouet class', () => {
  it('My first test runs', () => {

  })

  it('My second test fails', () => {
    
  })
})
